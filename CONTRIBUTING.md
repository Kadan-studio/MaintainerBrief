# Contributing

Please report bugs with the supported GitHub URL shape, what you expected, what happened, and the Chrome version when relevant. Do not include private page content.

Keep proposed changes focused. Before opening a change, run:

```sh
node --test tests/core.test.mjs
```

The v1 scope is intentionally narrow. Changes that add unrelated product features are unlikely to fit this release.
